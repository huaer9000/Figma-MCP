# Figma设计桥接 项目规则

本文件只管理当前项目。适用范围是 `/Users/lichao/Documents/Dev-project/Figma设计桥接` 及其子目录。

## 项目权威文件

- `AGENTS.md`：本项目的工作规则、权限边界和完成标准。
- `README.md`：项目介绍、当前阶段、使用方法、文档入口和相关 Skill。
- `CONTEXT.md`：压缩后的当前工作状态、近期决策、阻塞和下一步。
- `PROJECT.yaml`：机器可读的项目身份、类型和本地配置。
- `docs/`：项目长期有效的专项知识。

## 开工前

1. 阅读本文件。
2. 阅读 `README.md` 和 `CONTEXT.md`。
3. 只加载与当前任务相关的专项文档，避免无边界扩大上下文。
4. 检查当前任务是否缺少必要背景、权限或前置条件。
5. 如果无法按用户原需求完成，停止并解释原因、影响和建议，得到用户意见后再继续。

## 完成后

1. 验证实际结果，不能把“已配置”“已提交”或“进程正常”当作端到端完成。
2. 更新 `CONTEXT.md`，记录完成内容、证据、决策、阻塞和下一步。
3. 只有项目介绍、当前阶段、使用方法、文档入口或相关 Skill 变化时，才更新 `README.md`。
4. 根据项目专项规则更新对应文档，避免把所有内容塞进 README 或 CONTEXT。
5. 告知用户实际完成情况、验证范围和仍未完成的部分。

## 外部操作

- 发消息、发邮件、发布、部署、付款、发放优惠、删除数据等操作，默认需要用户明确授权。
- 只有本项目规则明确授予了范围、对象、额度和停止条件时，Agent 才能在授权范围内自动执行。
- 项目规则不得通过推断自行扩大外部操作权限。

## 文档冲突

- 用户当前明确指令优先于项目历史文档。
- `AGENTS.md` 管规则；`PROJECT.yaml` 管机器配置；专项文档管稳定知识；`README.md` 管入口和概况；`CONTEXT.md` 管当前状态。
- 如果同一事实在多个文件中冲突，不能静默挑选。先核对实际状态；无法核实时向用户说明冲突并询问。
- 修正冲突时同步更新引用该信息的文件，避免保留多个互相矛盾的版本。

## 项目暂停、恢复和归档

- 暂停：在 `README.md` 和 `PROJECT.yaml` 标记状态，在 `CONTEXT.md` 写明暂停原因、恢复条件和下一步。
- 恢复：先核对暂停期间的外部变化，再更新当前状态和计划。
- 归档：保留项目文件，不擅自删除；在 `CONTEXT.md` 形成最终交接摘要，并在 `README.md` 和 `PROJECT.yaml` 标记归档。
- 是否执行额外备份、发布或远程归档，由本项目后续规则决定。

## AGENTS.md 自我进化

本文件必须随项目持续演化，但只能吸收稳定、明确、属于本项目的规则。

### 规则晋升

- 用户明确说“以后”“每次”“必须”“不要”或“这个项目统一……”时，判断其是否属于本项目长期规则。
- 明确且只影响本项目的长期要求，可以直接更新本文件。
- 单次任务要求只记录到 `CONTEXT.md`，不升级为永久规则。
- 相同要求重复出现但长期性不明确时，在 `CONTEXT.md` 的“规则候选”中记录并向用户确认。
- 涉及外部操作、费用、权限、隐私、删除、部署或自动发布的规则，只有用户明确授权才能新增或扩大。
- 新规则与现有规则冲突时停止并询问，不擅自覆盖。
- 不因 Agent 自己的偏好或一次偶发错误增加永久规则。

### 更新动作

修改本文件后必须：

1. 告知用户新增、修改或删除了什么规则。
2. 在 `CONTEXT.md` 记录变化、原因和日期。
3. 检查 `README.md`、`PROJECT.yaml` 和专项文档是否需要同步。
4. 优先修改已有条目，避免重复规则。

## 开发项目规则

### 开发前

1. 阅读 `docs/PRODUCT.md`、`docs/TECH.md` 和与当前任务相关的 `docs/PITFALLS.md`。
2. 产品目标或技术方案不足以支撑开发时，先补齐并获得必要确认，不直接开始实现。
3. 按下方 Git 规则检查版本控制状态，保留用户已有改动。
4. 创建仓库、Issue、分支、commit、push、部署和发布的具体规则，以本项目后续明确规则为准；未明确授权时不执行外部操作。

### Git 工作区、版本快照与回退

1. 只要任务会修改文件，开始修改前先判断当前目录是否为 Git 仓库；纯问答、只读检查和解释任务不必执行这些命令。
2. 如果是 Git 仓库，修改前执行 `git status --short`、`git diff` 和 `git diff --cached`，检查未跟踪、未暂存和已暂存内容。向用户提供摘要，避免直接输出可能包含凭证或敏感数据的完整 diff。
3. 发现任务开始前已经存在的修改时，先说明涉及文件、主要内容和能够由证据支持的可能来源。Git 无法证明未提交修改由谁产生；无法判断时明确标记“来源未知”，不得猜测。
4. 既有修改与当前任务重叠或无法安全隔离时，停下来等待用户决定；与任务无关时保留原样继续，不得覆盖、清理、回退或混入本次修改。
5. 如果不是 Git 仓库，告知用户当前没有 Git 版本历史，不擅自执行 `git init`。需要保护认可版本时，先询问是否建立 Git 或创建经过校验的本地备份。
6. 用户确认某个版本可用、验收通过或准备开始下一轮明显修改时，按项目授权记录可定位的精确快照，例如 Git commit、tag 或经过校验的构建产物，并在 `CHANGELOG.md` 记录标识。
7. 修改一个已经验收或用户认可的版本前，必须确认快照真实存在。没有快照时先停止，询问是否创建本地 commit、tag 或备份；只有用户明确接受无法精确回退时才能继续。
8. 完成文件修改后，再次执行 `git status --short`、`git diff` 和 `git diff --cached`，向用户提供本次修改摘要、验证结果和建议的 commit 信息。
9. 未经用户明确确认，不执行 `git add`、`git commit`、`git tag` 或 `git push`。获得暂存授权后，只能暂存本次任务明确涉及的具体文件；禁止使用 `git add .`、`git add -A`、`git add -u` 等宽泛暂存命令。
10. “回退到上一版”必须恢复用户确认的明确 commit、tag 或构建快照，禁止根据聊天记忆、截图印象或功能描述重新手工实现一个“看起来像上一版”的版本。
11. 回退前先保留当前未提交改动；如果目标版本不明确，或回退可能覆盖用户改动，先向用户确认具体版本标识和影响。禁止使用 `git reset --hard` 或其他会丢失当前工作的方式。
12. 精确版本不仅包括源代码，还包括依赖锁文件、配置、数据库迁移、静态资源和会影响结果的外部状态。缺少其中关键部分时，要明确说明只能部分恢复。
13. 回退后核对版本标识，并重新执行对应测试、构建和必要的视觉或端到端验证；源码回到旧 commit 不等于最终效果已经恢复。

### 开发完成后

1. 执行与风险相称的测试和验证。
2. 按下方「文档生长」回写本次真正变化的文档。没有文档变化时，明确说明原因。
3. 将经过验证且未来可能复现的环境陷阱、错误方案和解决方式写入 `docs/PITFALLS.md`。
4. 普通报错、未经验证的猜测和一次性失误不进入 PITFALLS。
5. 按 Git 规则汇报最终工作区状态、修改摘要、验证结果和建议 commit 信息；未经确认不暂存、不提交、不打 tag、不 push。

### 文档生长

专项文档必须随真实变化变厚，但不能预建空文件，也不能只更新 `CONTEXT.md` 就结束。

1. 默认继续写在 `docs/PRODUCT.md`、`docs/TECH.md`、`docs/DECISIONS.md` 和 `docs/PITFALLS.md`。功能验收或用户确认可合并后，必须回写本次真正变化的那几份；只更新 `CONTEXT.md` 或只记 `CHANGELOG.md` 不算完成。
2. `CHANGELOG.md` 记录用户可感知的变化。产品目标、范围、流程或审美偏好的变化写入 `docs/PRODUCT.md`。架构、数据、接口、安全或部署的变化写入 `docs/TECH.md`。影响长期实现方式的选择写入 `docs/DECISIONS.md`。
3. 审美与偏好只写本项目已经明确、且会与默认实现分叉的口味。跨项目稳定偏好继续放在全局规则或 Skill，不要抄进本项目。
4. 鉴权、数据模型、上线检查清单、项目专属编码习惯，默认写在现有文档对应章节。只有该领域已经独立复杂、并且后续任务会反复单独查阅时，才拆成专项文件，并同步更新 `README.md` 的文档导航。
5. 尚未发生的事故、尚未确定的库表、尚未执行过的上线检查、尚未出现的项目专属习惯，不要预建空文件。项目专属编码习惯被确认后，再创建 `docs/CONVENTIONS.md`。
